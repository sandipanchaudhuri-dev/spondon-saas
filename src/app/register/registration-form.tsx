"use client";import {useState} from "react";import Link from "next/link";import {z} from "zod";
const schema=z.object({pujo_name:z.string().min(2),address:z.string().min(8),primary_contact_number:z.string().min(8),theme:z.string().optional(),artist_name:z.string().optional(),contact_person_name:z.string().min(2),email:z.string().email(),whatsapp_number:z.string().min(8),terms:z.literal(true)});
export function RegistrationForm(){const [busy,setBusy]=useState(false);const [error,setError]=useState("");const [reference,setReference]=useState("");async function submit(formData:FormData){setError("");const raw=Object.fromEntries(formData);const parsed=schema.safeParse({...raw,terms:raw.terms==="on"});if(!parsed.success){setError("Please complete all required fields with valid contact details.");return}setBusy(true);try{const res=await fetch("/api/registrations",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify(parsed.data)});const body=await res.json();if(!res.ok)throw new Error(body.error||"Registration could not be submitted.");setReference(body.reference_id)}catch(e){setError(e instanceof Error?e.message:"Something went wrong.")}finally{setBusy(false)}}if(reference)return <section className="card form-card" style={{textAlign:"center"}}><div style={{fontSize:50}}>✓</div><h2>Registration received</h2><p className="muted">Keep this reference for your records.</p><div className="alert success" style={{fontSize:20,fontWeight:800}}>{reference}</div><Link className="button secondary" href="/">Return home</Link></section>;return <form className="card form-card" action={submit}><div className="fields"><div className="field full"><label>Organisation / Pujo name *</label><input className="input" name="pujo_name" autoComplete="organization" required/></div><div className="field full"><label>Address *</label><textarea name="address" required/></div><div className="field"><label>Contact person *</label><input className="input" name="contact_person_name" autoComplete="name" required/></div><div className="field"><label>Primary contact number *</label><input className="input" name="primary_contact_number" inputMode="tel" required/></div><div className="field"><label>Email *</label><input className="input" name="email" type="email" autoComplete="email" required/></div><div className="field"><label>WhatsApp number *</label><input className="input" name="whatsapp_number" inputMode="tel" required/></div><div className="field"><label>Theme</label><input className="input" name="theme"/></div><div className="field"><label>Artist name</label><input className="input" name="artist_name"/></div><div className="field full terms"><label style={{display:"flex",gap:10,alignItems:"flex-start"}}><input name="terms" type="checkbox" required style={{marginTop:3}}/><span>I confirm the information is correct and accept the programme rules. Selection decisions are final, and participating committees must follow applicable government and safety protocols.</span></label></div></div>{error&&<div className="alert error">{error}</div>}<button className="button" disabled={busy} style={{width:"100%",marginTop:22}}>{busy?"Submitting…":"Submit registration"}</button></form>}
